name: Repository trust policy gate

on:
  pull_request:
    paths:
      - 'SECURITY.md'
      - 'CONTRIBUTING.md'
      - 'tests/test_repository_trust_policy.py'
      - '.github/workflows/**'
  push:
    branches: [main]
    paths:
      - 'SECURITY.md'
      - 'CONTRIBUTING.md'
      - 'tests/test_repository_trust_policy.py'
      - '.github/workflows/**'

permissions:
  contents: read

concurrency:
  group: repository-trust-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout without retained credentials
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.11'

      - name: Verify bounded trust policies
        run: python -m unittest discover -s tests -p 'test_repository_trust_policy.py' -v

      - name: Reject whitespace errors
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          set -euo pipefail
          git cat-file -e "${BASE_SHA}^{commit}"
          git diff --check "${BASE_SHA}...HEAD"
